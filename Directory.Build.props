<Project>

  <!-- Версия у всех проектов одна и задаётся здесь.
       Раньше она стояла только в ProxyCage.Cli, а «chp version» читает сборку Core,
       поэтому продукт после выпуска показывал старый номер и не видел обновления. -->
  <PropertyGroup>
    <Version>1.1.0</Version>
  </PropertyGroup>

</Project>
